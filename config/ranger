oa 时间排序 ob名字排序
dD 删除
dd 剪切 pp 粘贴
<CR> 打开, py txt 会自动进入 vim , jpg 图片自动打开 pdf 需要手动输出  arch也是
<space> 圈选

导航
gh home目录 

+x+w+r  权限

cd快速跳转
cw重命名

r open with,自定义的打开方式 详见 ~/.config/ranger/rifile.conf 中label定义 不过系统中的okular firefox好像不需要定义就可以用
有机会加一些比如压缩之类的快捷方式进去,mpd其实也可以加进来，没有独立ui的必要,不过独立稍微比起开ranger方便，但是要另外写程序就不划算了

#preview 好像要搞定一个scope.sh的脚本,不知道是不是我的ranger版本不对
#虽然没找到scope.sh,不过安装了下面的东西之后，preview还是正常工作的
sudo apt install lynx elinks highlight 
sudo apt install -y caca-utils #img2txt 
sudo apt install -y atool mediainfo -y
#pdftotext

比如查找功能之类的，按键和vim很像

Fri 27 May 2016 09:54:41 AM NZST
最近主要对比下ranger 和nautilus之类gui的差异，反正nautilus什么的本身就不好用，ranger反而顺畅得多。
不过图片批量preview，ranger是做不到的
一个方案是，写一个ranger脚本，获取一个目录中的所有图片，生成html，然后打开firefox,但是这样没有双向同步的文件操作

Wed 08 Jun 2016 03:09:42 PM NZST
作为文件管理器这样的核心软件ranger和很多命令行工具整合了.
这种交错程度大概不比一般的DE低.
所以通过ranger,可以找出不少有用的命令行工具

Wed 08 Jun 2016 03:40:14 PM NZST
隐藏用户名
cd /usr/local/lib/python2.7/dist-packages/ranger/gui/widgets/
sudo vim titlebar.py     
line 92     def _get_left_part(self, bar):
comment
#        bar.add(self.fm.username, 'hostname', clr, fixed=True)
#        bar.add('0', 'hostname', clr, fixed=True)
#        bar.add(self.fm.hostname, 'hostname', clr, fixed=True)
#        bar.add(' ', 'hostname', clr, fixed=True)
sudo vim statusbar.py 
line 105
uncomment
#         self._get_left_part(bar)
