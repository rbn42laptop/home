#!/bin/bash
#根据compiz viewports的背景选择相适合的term
#有个问题是,这台电脑上开机时间长,python就容易崩溃,
#所以开机时间长可能会导致term用不了.

#这台电脑没法用,频繁运行python会crash
color=`python ~/bin/compiz/get_wall_color.py`
if [ "$color" == "white" ];then
    t=~/bin/term_white
else
    t=~/bin/term_shadow
fi
bash $t "$@"
