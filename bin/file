#!/bin/bash
source ~/bin/GLOBAL
#记录到rofi
python ~/bin/add2rofi.py "$ROFI_HISTORY" 1 ./bin/file "$@"
#nautilus 

if [ $# -eq 0 ]
  then
    ~/bin/term -e "ranger "
  else
    ~/bin/term -e "ranger \"$@\""
fi

